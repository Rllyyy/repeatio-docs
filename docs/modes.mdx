---
sidebar_position: 6
sidebar_label: Modi
---

import PracticeMode from "../static/img/practice-mode.png";
import BookmarkedMode from "../static/img/bookmarked-mode.png";

# Modi

:::info In diesem Kapitel lernst du:

- Welche Übungsmodi es gibt

:::

Aktuell werden zwei Modi unterstützt:

- [Übung (Practice)](#üben-practice) und
- [gemerkte Fragen (Bookmarked)](#gemerkte-fragen-bookmarked-questions).

## Üben (Practice)

Im Modus Practice werden alle Fragen abgefragt.  
Klick in der Karte Practice auf `Start` oder `Random`. Im Modus `Random` werden die Fragen zufällig abgefragt. Bei `Start` werden die Fragen chronologisch abgefragt.

<img src={PracticeMode} width={440} style={{ display: "block", marginBottom: "20px" }} />

:::tip Tipp

Nutze die sich kreuzenden Pfeile (`🔀`), um zwischen der chronologischen und zufälligen Reihenfolge zu wechseln.

:::

## Gemerkte Fragen (Bookmarked Questions)

In diesem Modus werden nur Fragen abgefragt, die zuvor markiert wurden.  
Klick dazu in der Karte Bookmarked Questions auf `Start`.

<img src={BookmarkedMode} width={460} style={{ display: "block", marginBottom: "20px" }} />

:::tip Tipp
Klick auf die sich kreuzenden Pfeile (`🔀`), um die Fragen zufällig zu sortieren.
:::

:::tip Tipp
Durch Klicken auf die drei Punkte (`⋮`) rechts neben der Karte Bookmarked Questions können die gemerkten Fragen exportiert, importiert oder gelöscht werden.
:::
