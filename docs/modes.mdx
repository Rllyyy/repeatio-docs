---
sidebar_position: 2
sidebar_label: Übungsmodi
---

import PracticeMode from "../static/img/practice-mode.png";
import BookmarkedMode from "../static/img/bookmarked-mode.png";

# Modi

:::info In diesem Kapitel lernst du:

- Welche Übungsmodi es gibt

:::

Aktuell werden zwei Modi unterstützt:

- [Übung (Practice)](#üben-practice) und
- [gemerkte Fragen (Bookmarked)](#gemerkte-fragen-bookmarked-questions).

## Üben (Practice)

Im Modus Practice werden alle Fragen abgefragt.  
Navigiere zu dem Modul, das du trainieren möchtest und klick in der Karte Practice auf `Start` oder `Random`.  
Bei `Random` werden die Fragen zufällig abgefragt. Bei `Start` werden die Fragen chronologisch abgefragt.

<img src={PracticeMode} width={480} style={{ display: "block", marginBottom: "40px" }} />

:::tip Tipp

Es ist jederzeit möglich durch einen klick auf die sich kreuzenden Pfeile (`🔀`) zwischen einer chronologischen und zufälligen Reihenfolge zu wechseln

:::

## Gemerkte Fragen (Bookmarked Questions)

In diesem Modues werden nur Fragen abgefragt, die du zuvor markiert hast.  
Um Fragen in diesem Modus zu trainieren, klicke in einem Modul in der Karte Bookmarked Questions auf `Start`.

<img src={BookmarkedMode} width={480} style={{ display: "block", marginBottom: "40px" }} />

:::tip Tipp
Klick auf die sich kreuzenden Pfeile (`🔀`), um die Fragen zufällig zu sortieren.
:::

:::tip Tipp
Mit einem Klick auf die drei Punkte (`⋮`) auf der rechten Seite der Karte, kannst du die gemerkten Fragen exportieren, importieren oder löschen.
:::
