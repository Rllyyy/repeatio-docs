---
sidebar_position: 5
sidebar_label: Fragen-Editor
---

import QuestionEditor from "../static/img/question-editor.png";

# Fragen-Editor

:::info In diesem Kapitel lernst du:

- Wie der Fragen-Editor aufgebaut ist
- Wie man Fragen formatiert
- Wie man Bilder, Tabellen und Formeln hinzufügt

:::

Mit dem Fragen-Editor können Fragen [erstellt](./questions/add-question) und [bearbeitet](./questions/edit-question) werden.

## Elemente

Die mit einem roten Sternchen markierten Felder sind Pflichtfelder.
Alle anderen Felder sind optional. Die Felder **Title**, **Help** und **Answer** können mit MarkDown [gestaltet](#styling) werden.

<img src={QuestionEditor} width={620} style={{ display: "block", marginBottom: "40px", marginInline: "auto" }} />

### ID

Die ID ist eine eindeutige Kennung für die Frage. Sie muss **einzigartig** in dem Modul sein und darf **keine Leer- oder Sonderzeichen** (#, /, !, ...) enthalten. Verwendet anstelle eines Leerzeichens einen Binde- oder Unterstrich.

### Title (Titel)

Formuliere den Titel so, dass er die Fragestellung klar und präzise wiedergibt. Im Titel kann MarkDown verwendetet werden, um den Text zu formatieren, Bilder einzufügen oder Tabellen zu erstellen.

### Points (Punkte)

Gib die Anzahl der Punkte an, die die Frage wert ist.

### Help (Hilfe)

Im Help-Text können zusätzliche Informationen zur Fragestellung oder zur Beantwortung der Frage hinzufügt werden. Der Help-Text wird unterhalb der Fragestellung angezeigt. Auch im Help-Text kann MarkDown verwendetet werden, um den Text zu formatieren, Bilder einzufügen oder Tabellen zu erstellen.

### Type (Typ)

Wähle den Typ der Frage aus. Aktuell gibt vier verschiedene Typen: **[Multiple Choice](/question-types#multiple-choice)**, **[Multiple Response](/question-types#multiple-response)**, **[Gap Text (Lückentext)](/question-types#lückentext-gap-text)** und **[Extended-Match (Erweiterte Zuordnung)](/question-types#erweiterte-zuordnungsaufgabe-extended-match)**.

### Answer (Antwort)

In diesem Bereich wird die korrekte Lösung der Frage eingetragen. Wähle dazu zunächst den Typ der Antwort aus.
Auch im Antwort-Text kann MarkDown verwendetet werden, um den Text zu formatieren, Bilder einzufügen oder Tabellen zu erstellen.

#### Multiple Choice

Mit dem Plus-Button (`➕`) können weitere Antwortmöglichkeiten hinzugefügt werden.  
Klick zum Löschen einer Antwortmöglichkeit auf diese und dann auf den Minus-Button (`➖`).  
Ist beim Klick auf den Minus-Button keine Antwortmöglichkeit ausgewählt, wird die letzte Antwortmöglichkeit gelöscht.
Wähle die korrekte Antwort durch einen Klick auf den Kreis aus. Falls mehrere Antwortmöglichkeiten korrekt sind, wähle stattdessen den Typ Multiple Response.

#### Multiple Response

Mit dem Plus-Button (`➕`) können weitere Antwortmöglichkeiten hinzugefügt werden.  
Klick zum Löschen einer Antwortmöglichkeit auf diese und dann auf den Minus-Button (`➖`).  
Ist beim Klick auf den Minus-Button keine Antwortmöglichkeit ausgewählt, wird die letzte Antwortmöglichkeit gelöscht.
Wähle die korrekten Antworten durch einen Klick auf die Rechtecke aus. Falls nur eine Antwortmöglichkeit korrekt ist, wähle stattdessen den Typ Multiple Choice.

#### Gap Text

Beim Lückentext muss die korrekte Antwort zwischen zwei rechteckigen Klammern (`[ ]`) eingetragen werden.
Diese können über die Tastatur oder mit dem Plus-Button (`➕`) hinzugefügt werden.  
Sind mehrere Antworten möglich (z.B. verschiedene Synonyme oder Schreibweisen), können diese durch ein Semikolon (`;`) getrennt werden (Beispiel: `[Antwort1; Antwort2; Antwort3]`).
Lücken können per Tastatur oder durch einen Klick auf das Minus-Button (`➖`) gelöscht werden, wenn der Text markiert wurde.

:::tip Tipp
Mit MarkDown ist es möglich, Lücken in Tabellen oder Listen zu erstellen.
:::

#### Extended-Match

Nutze zum Hinzufügen neuer Optionen auf der jeweiligen Seite den Plus-Button (`➕`) in der Antwort.  
Verbinde richtige Optionen durch einen Klick auf den Kreis neben der Option.  
Optionen und Verbindungen können durch einen Klick auf den Kreuz-Symbol (`✖`) gelöscht werden.

## Styling

Repeatio unterstützt die Gestaltung mit `Markdown` und `HTML` für die folgenden Elemente: **Titel**, **Hilfe** und **Antwortmöglichkeiten**.

### Textformatierung (fett, kursiv, durchgestrichen, Code, Unterstrichen)

<table>
  <tr>
    <th>Typ</th>
    <th>Markdown</th>
    <th>HTML</th>
    <th>Resultat</th>
  </tr>
  <tr>
    <td>Fett</td>
    <td>
      <code>**Text**</code>
    </td>
    <td>
      <code>&lt;b&gt;Text&lt;/b&gt;</code>
    </td>
    <td>
      <b>Text</b>
    </td>
  </tr>
  <tr>
    <td>Kursiv</td>
    <td>
      <code>*Text*</code> or <code>_Text_</code>
    </td>
    <td>
      <code>&lt;i&gt;Text&lt;/i&gt;</code>
    </td>
    <td>
      <i>Text</i>
    </td>
  </tr>
  <tr>
    <td>Durchgestrichen</td>
    <td>
      <code>~~Text~~</code>
    </td>
    <td>
      <code>&lt;s&gt;Text&lt;/s&gt;</code>
    </td>
    <td>
      <s>Text</s>
    </td>
  </tr>
  <tr>
    <td>Code</td>
    <td>
      <code>`Text`</code>
    </td>
    <td>
      <code>&lt;code&gt;Text&lt;/code&gt;</code>
    </td>
    <td>
      <code>Text</code>
    </td>
  </tr>
  <tr>
    <td>Unterstrichen</td>
    <td></td>
    <td>
      <code>&lt;u&gt;Text&lt;/u&gt;</code>
    </td>
    <td>
      <u>Text</u>
    </td>
  </tr>
</table>

### Zeilenumbrüche

<p>
  Zeilenumbrüche können in MarkDown durch Drücken der <code>Enter</code> Taste erreicht werden. Um einen Zeilenumbruch
  in HTML zu erreichen, muss <code>&lt;br /&gt;</code> verwendet werden.
</p>

<table>
  <thead>
    <tr>
      <th>Typ</th>
      <th>Markdown</th>
      <th>HTML</th>
      <th>Resultat</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Einzelner Zeilenumbruch</td>
      <td>
        Oberer Text <br /> Neue Zeile
      </td>
      <td>
        Oberer Text <code>&lt;br /&gt;</code> Neue Zeile
      </td>
      <td>
        Oberer Text <br />
        Neue Zeile
      </td>
    </tr>
    <tr>
      <td>Mehrere Zeilenumbrüche</td>
      <td>
        Text in mehrere<code>&ensp;</code>
        <sup>
          <a href='#footnote-line-break-1'>[1]</a>
        </sup>
        <br />
        Zeilen aufgeteilt
      </td>
      <td>
        Text in mehrere <code>&lt;br /&gt;&lt;br /&gt;</code>Zeilen aufgeteilt
      </td>
      <td>
        Text in mehrere <br /> <br /> Zeilen aufgeteilt
      </td>
    </tr>
  </tbody>
</table>

<p id='footnote-line-break-1'>[1]: Beachte die zwei Leerzeichen!</p>

### Listen

Es gibt zwei Arten von Listen: **Ungeordnete** und **Geordnete** Listen.

  <table>
    <tr>
      <th>Typ</th>
      <th>Markdown</th>
      <th>HTML</th>
      <th>Resultat</th>
    </tr>
    <tr>
      <td>Ungeordnete Listen</td>
      <td>

```markdown
Text...

- Erstes ungeordnetes Element
- Ein weiteres Element
- Letztes Element

Text außerhalb der Liste
```

</td>
      <td>

```html
Text...
<ul>
  <li>Erstes ungeordnetes Element</li>
  <li>Ein weiteres Element</li>
  <li>Letztes Element</li>
</ul>
Text außerhalb der Liste
```

  </td>
      <td>
        <p>Text...</p>
        <ul>
          <li>Erstes ungeordnetes Element</li>
          <li>Ein weiteres Element</li>
          <li>Letztes Element</li>
        </ul>
        <p>Text außerhalb der Liste</p>
      </td>
    </tr>
    <tr>
      <td>Geordnete Listen</td>
<td>

```markdown
Text...

1. Erstes geordnetes Element
2. Ein weiteres Element
3. Letztes Element

Text außerhalb der Liste
```

</td>
      <td>

```html
Text...
<ol>
  <li>Erstes geordnetes Element</li>
  <li>Ein weiteres Element</li>
  <li>Letztes Element</li>
</ol>
Text außerhalb der Liste
```

  </td>
      <td>
        <p>Text...</p>
        <ol>
          <li>Erstes geordnetes Element</li>
          <li>Ein weiteres Element</li>
          <li>Letztes ElementLetztes Element</li>
        </ol>
        <p>Text außerhalb der Liste</p>
      </td>
    </tr>
  </table>

### Tabellen

  <table>
    <tr>
      <th>Markdown</th>
      <th>HTML</th>
      <th>Resultat</th>
    </tr>
    <tr>
      <td>

```markdown
Text

| Überschrift 1 | Überschrift 2 |
| ------------- | :-----------: |
| Element 1     |   Element 2   |
| Element 3     |   Element 4   |

Text außerhalb der Tabelle
```

</td>
  <td>

```html
Text
<table>
  <tr>
    <th>Überschrift 1</th>
    <th style="text-align:center">Überschrift 2</th>
  </tr>
  <tr>
    <td>Element 1</td>
    <td style="text-align:center">Element 2</td>
  </tr>
  <tr>
    <td>Element 3</td>
    <td style="text-align:center">Element 4</td>
  </tr>
</table>
Text außerhalb der Tabelle
```

</td>
      
      <td>
Text

| Überschrift 1 | Überschrift 2 |
| ------------- | :-----------: |
| Element 1     |   Element 2   |
| Element 3     |   Element 4   |

Text außerhalb der Tabelle

</td>
</tr>

  </table>

:::tip Tipp
Benutze die folgendene Syntax unterhalb der Überschrift, um Elemente in einer MarkDown Tabelle auszurichten:

- Links (`| :------- |`) ist optional, da es identisch zu `| ------- |` ist
- Mitte (`| :-------: |`)
- Rechts (`| -------: |`)

:::

### Bilder

Bilder können mit der Syntax `![Alt Text](www.link-to-image.com)` hinzufügen werden.

:::info Info
Der Alt Text wird angezeigt, wenn das Bild aus irgendeinem Grund nicht geladen werden kann, und dient auch der Barrierefreiheit für Sehbehinderte.
:::

  <table>
    <tr>
      <th>Markdown</th>
      <th>HTML</th>
      <th>Resultat</th>
    </tr>
    <tr>
      <td>

```markdown
![md](link-to-image)
```

</td>
  <td>

```html
<img src="link-to-image" alt="md" />
```

</td>
      <td>
        <img src="https://upload.wikimedia.org/wikipedia/commons/d/d9/Markdown.png" alt="md" style={{height: "48px"}} />
      </td>
    </tr>
  </table>

### CSS

Benutze inline CSS in Kombination mit HTML, um ein Element zu stylen.

<table>
  <tr>
    <th>Input</th>
    <th>Resultat</th>
  </tr>
  <tr>
    <td>

```markdown
Dieser Text ist nicht beeinflusst von CSS

<p style="color: red">Dieser Text ist rot</p>
```

</td>
    <td>
      <p>Dieser Text ist nicht beeinflusst von CSS</p>
      <p style={{color: "red"}}>Dieser Text ist rot</p>
    </td>
  </tr>
</table>

### Formeln (LaTeX)

Repeatio benutzt LaTeX, um Formeln zu rendern.  
Schreibe die Funktion zwischen zwei Dollarzeichen (`$`).

<table>
  <tr>
    <th>Input</th>
    <th>Resultat</th>
  </tr>
  <tr>
    <td>

```markdown
$x =\frac{\sqrt{20}}{3\times(5-10)^2}$
```

</td>
    <td>

$x =\frac{\sqrt{20}}{3\times(5-10)^2}$

</td>

  </tr>
</table>

**[Hier](https://github.com/Rllyyy/repeatio/blob/main/.github/Docs/LaTeX.md)** ist eine detaillierte Anleitung.
